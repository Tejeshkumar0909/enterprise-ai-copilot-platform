# Deployment Model

The service is containerized with Docker and can be deployed behind an API gateway. CI validates tests before publishing an image. Runtime monitoring should track latency, empty retrievals, and groundedness metrics.
