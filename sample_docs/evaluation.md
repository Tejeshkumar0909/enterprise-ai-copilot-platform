# Evaluation Workflow

The evaluation workflow checks retrieval relevance, citation coverage, and answer consistency. Golden question-answer pairs are run during CI to catch regressions. Low-scoring responses are reviewed and the retriever configuration is tuned before release.
